(def-symbol (delay ex) (lambda () ex))
(def (force de:function) (de))
(def (curry f:function n:int)
    (def (curry-rec i:int l:list)
        (if (= i 0)
            (apply-list f l)
            (lambda (a)
                (curry-rec (- i 1) (list-add l a)))))
    (curry-rec n (list)))
